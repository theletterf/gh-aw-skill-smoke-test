# Fake document

This page exists only to test the `gh-aw` docs skill smoke workflow.

It should stay simple on `main` so a test PR can add an obvious style-guide issue and the workflow can review that change.
